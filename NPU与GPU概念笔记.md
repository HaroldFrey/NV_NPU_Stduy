# NPU 与 GPU 概念笔记

> 学习目的：厘清 NPU 与 GPU 的本质区别与演进趋势，定位自研 NPU 在架构光谱上的位置，理解验证重点
> 创建日期：2026-08-12

---

## 0. 一句话定位

**NPU 是"只为神经网络优化"的专用处理器；GPU 是"什么都能跑"的通用并行处理器。两者的取舍核心是：灵活性 vs 效率。**

---

## 1. NPU 是什么

**NPU = Neural Processing Unit（神经网络处理器）**，为神经网络计算专门设计的处理器。它不是某一家公司的专有名词，而是一大类芯片的统称：

| 厂商/项目 | 自家叫法 |
|---|---|
| 华为 | NPU（昇腾 AI Core） |
| Google | TPU（Tensor Processing Unit） |
| NVIDIA | DLA / Tensor Core（不叫 NPU） |
| Graphcore | IPU |
| 地平线 | BPU |
| 寒武纪 | 智能处理器（MLU） |

学术上统称 **DSA（Domain-Specific Accelerator，领域专用架构）**——为特定计算形态设计，用**牺牲通用性换效率**。

**NPU 的核心设计特征**：

1. **基本操作是张量/矩阵**——MAC 阵列、脉动阵列，而不是通用指令
2. **大数据缓冲 + 数据复用**——片上 SRAM 很大（NVDLA：512KB CBUF + 512KB CMEM 配 2048 MAC），访存开销是设计重点
3. **低精度计算**——主打 INT8/FP16 + 量化，算力/能效比高
4. **专用数据流/流水线**——算子是"硬件形态"，不是"程序形态"
5. **面向推理为主**——per-layer 编程、编译器静态调度（NVDLA 就是典型）

---

## 2. GPU 是什么（对照着看）

**GPU = Graphics Processing Unit**，从图形渲染演化而来：

- **SIMT 架构**：大量通用执行单元（SM = Streaming Multiprocessor）并行执行 warp 指令（32 线程/warp）
- **通用可编程**：CUDA/OpenCL 写任意算法，不限于神经网络
- 现代 GPU 是"**通用核 + 专用核**"混合体：
  - **CUDA Core**：通用标量计算
  - **Tensor Core**：矩阵乘加专用单元——**本质上就是嵌在 GPU 里的"NPU"**

---

## 3. 核心区别

| 维度 | GPU | NPU |
|---|---|---|
| 起源 | 图形渲染 → 通用并行计算 | 神经网络计算 |
| 计算单元 | 通用 ALU（+少量 Tensor Core） | 大量 MAC / 专用阵列 |
| 编程模型 | SIMT 通用指令流，任意算法 | 专用算子 / per-layer 配置 |
| 灵活性 | 高 | 低（主要干神经网络） |
| 能效 | 同算力下功耗高 | 同算力下更省电、更便宜 |
| 精度 | FP32/FP64 全支持 | 主打 INT8/FP16 量化 |
| 片上存储 | 缓存层次为主 | 大块显式 SRAM 缓冲 |
| 编程难度 | CUDA 通用编程 | 专用编译器（TVM/TensorRT） |

**一句话总结**：GPU 是"什么都能跑的通用并行机，但为通用性付了功耗和成本"；NPU 是"只为神经网络优化的专用机，用灵活性换效率和能效"。

---

## 4. 边界正在模糊（关键认知）

现代趋势是**互相靠近**：

```
完全通用 ◄─────────────────────────────── 完全专用
GPU(纯SIMT)   GPU+TensorCore   兼容CUDA的自研NPU   NVDLA式NPU
                                   ← 你们的位置
```

- **GPU 在变 NPU 化**：H100/B200 的 Tensor Core 占比越来越大，AI 计算越来越"专用"
- **NPU 在变 GPU 化**：兼容 CUDA 工具链的自研 NPU 按"指令流 + CUDA 编程模型"工作，而不是"寄存器配置流水线"

**结论**：NPU 与 GPU 不是非黑即白，而是"通用计算 → 专用加速"光谱上的不同位置。

---

## 5. 对验证工作的意义（自研 NPU 视角）

你们的 NPU 定位在光谱中间——同时有 GPU 属性（指令语义、warp/调度、通用性）和 NPU 属性（专用单元、数据流、低精度）：

| 属性来源 | 具体表现 | 验证影响 |
|---|---|---|
| GPU 属性（CUDA 兼容） | 指令流、SIMT 语义、warp/内存模型 | 验证重点是**指令语义正确性**（翻译层） |
| NPU 属性（专用加速） | 专用计算单元、数据流、低精度 | 验证重点是**算子/数据通路正确性** |

这也解释了本目录各笔记的定位：

- [NVDLA架构学习笔记.md](NVDLA架构学习笔记.md) —— "纯专用"NPU 的极端形态，理解设计取舍的参照系
- [CUDA软件栈概念笔记.md](CUDA软件栈概念笔记.md) —— GPU 软件链（CUDA/PTX/SASS/CUBIN），CUDA 语义源头
- [NVIDIA工具链与自研NPU验证笔记.md](NVIDIA工具链与自研NPU验证笔记.md) —— 兼容层验证方法（最接近你们项目）

---

## 6. 记忆锚点

1. **NPU = 专用**（矩阵/数据流/低精度/省电），**GPU = 通用**（指令流/任意算法/高功耗）
2. **Tensor Core = GPU 里的 NPU**；**兼容 CUDA 的 NPU = NPU 里的 GPU**
3. 一切架构取舍都在"灵活性 vs 效率"这根轴上
