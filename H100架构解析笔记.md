# H100（Hopper）架构解析笔记

> 学习目的：H100 是自研 NPU 的参考架构，需要理清架构层级、各组件意义、数据流动、对外接口与基础概念
> 创建日期：2026-08-12（初版）
> 更新日期：2026-08-12（依据官方白皮书《NVIDIA H100 Tensor Core GPU Architecture》+ 官方博客 Hopper In-Depth 全面修订，修正性能数字）
> 说明：正文性能数字为白皮书数字（2022 年初版），末尾标注与最终上市规格的差异

---

## 0. 一句话定位

**H100 = Hopper 架构数据中心 GPU**（第九代，GH100 芯片，2022）。它是"GPU NPU 化"的集大成者：在通用 SIMT 计算单元（SM）旁，放上**大规模专用矩阵引擎（Tensor Core）+ 异步张量搬运引擎（TMA）**，AI 计算能力（FP8 2,000 TFLOPS 稠密）远超通用计算（FP32 60 TFLOPS）。

**自研 NPU 参考它的逻辑**：H100 的 AI 计算部分（Tensor Core + TMA + SMEM 复用）本质上就是一个巨型 NPU 与通用 GPU 的组合——参考它 = 参考"已验证的 NPU 子系统设计"。

---

## 1. 芯片总览：三级物理层级

GH100 芯片按 **GPC → TPC → SM** 三级组织（理解 GPU 布局的基础）：

```text
GH100 芯片（TSMC 4N，800 亿晶体管，814 mm²）
 └── 8 × GPC（图形处理簇）
      └── 每 GPC 9 × TPC（纹理处理簇）
           └── 每 TPC 2 × SM（流多处理器）→ 全片 144 SM
```

| 型号 | GPC | TPC | SM | Tensor Core |
| --- | --- | --- | --- | --- |
| GH100 全片 | 8 | 72（9/GPC） | 144 | 576 |
| **H100 SXM5** | 8 | 66 | **132** | 528 |
| H100 PCIe | 7~8 | 57 | 114 | 456 |

- H100 面向数据中心计算（AI/HPC），**不做图形**：只有 2 个 TPC 保留图形能力
- GPC 是硬件上"物理相邻"的 SM 集合——簇（Cluster）调度就在 GPC 内进行

**关键规格（H100 SXM5，白皮书数字）**：

| 项 | 数值 |
| --- | --- |
| 制程 / 晶体管 / 面积 | TSMC 4N / 800 亿 / 814 mm² |
| SM / Tensor Core | 132 / 528（4/SM） |
| 算力（稠密/稀疏，白皮书 V1.04 最终） | FP8 **1,979/3,958** TFLOPS；FP16·BF16 989/1,979；TF32 495/989；INT8 1,979/3,958 TOPS |
| 通用算力 | FP32 67 TFLOPS；FP64 33.5 TFLOPS（非张量） |
| 显存 | 80GB HBM3（5 栈，5120-bit），**3,352 GB/s** |
| L2 | 50MB（A100 的 1.25 倍） |
| NVLink 4 | 900 GB/s（18 条链路） |
| PCIe | Gen5 x16，128 GB/s |
| TDP | 700W（SXM） |
| Compute Capability | 9.0 |

> 📌 **规格版本说明**：本表为白皮书 V1.04（已含最终时钟与最终算力）。Boost 时钟：Tensor Core 运算 1,830 MHz，其余运算 1,980 MHz。注意部分营销资料把 FP8 3,958 当"稠密"引用——白皮书口径为**稠密 1,979 / 稀疏 3,958**（稀疏 = 2:4 结构化稀疏翻倍）。

---

## 2. SM：基本执行单元（组件的意义）

SM（Streaming Multiprocessor）= 一个独立可调度、可执行指令的处理单元，**全片 132 个 SM 各自独立工作**。理解 SM = 理解 GPU 如何并行。

```text
┌────────────────────────── SM（流多处理器）──────────────────────────┐
│  256KB 寄存器堆（线程私有）                                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                  │
│  │ 子分区0 │ │ 子分区1 │ │ 子分区2 │ │ 子分区3 │  ← 4 个处理子分区   │
│  │1调度器   │ │1调度器   │ │1调度器   │ │1调度器   │                  │
│  │32 FP32  │ │32 FP32  │ │32 FP32  │ │32 FP32  │                  │
│  │16 FP64  │ │16 FP64  │ │16 FP64  │ │16 FP64  │                  │
│  │16 INT32 │ │16 INT32 │ │16 INT32 │ │16 INT32 │                  │
│  │1 Tensor │ │1 Tensor │ │1 Tensor │ │1 Tensor │                  │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                  │
│  228KB 共享内存/L1（可配置） + 32 LD/ST 单元                        │
│  TMA 引擎（异步张量搬运，1 个/SM）                                   │
└─────────────────────────────────────────────────────────────────────┘
```

| 组件 | 数量/容量 | 意义 |
| --- | --- | --- |
| 处理子分区 | 4 个，各带 1 个 warp 调度器 | 每周期每调度器发 1 条 warp 指令（32 线程）→ SM 每周期 4 条 |
| FP32 核心 | 128（4×32） | 通用标量计算（通用性所在） |
| FP64 / INT32 核心 | 64 / 64 | 双精度 HPC、整数寻址 |
| **Tensor Core** | 4（每子分区 1 个） | **矩阵乘加专用引擎（NPU 核心对应物）** |
| 寄存器堆 | 256KB | 线程私有，最快存储，决定占用率 |
| **共享内存 SMEM/L1** | **合计 256KB（SMEM 最大可配 228KB）** | **块内共享的片上显式存储——AI 计算的主战场** |
| LD/ST 单元 | 32 | 访存指令执行 |
| **TMA 引擎** | 1 | **异步张量搬运（数据喂养 Tensor Core 的关键）** |
| 执行容量 | 2048 线程 / 64 warp / 32 块 | 并行度上限 |

**基础概念**：一个 warp = 32 线程，SIMT 模式——同一条指令被 32 线程用不同数据执行。线程块（CTA）调度到一个 SM 上，块内线程共享 SMEM、用 barrier 同步。

---

## 3. Tensor Core 与精度体系（AI 计算引擎）

### 3.1 第 4 代 Tensor Core

- 计算形式：**MMA（矩阵乘加）**，`D = A×B + C`，一次指令完成整块矩阵运算（而非逐元素）
- 每 SM 4 个，每周期吞吐是 A100 SM 的 2 倍（同精度），**FP8 是 4 倍**
- 支持精度：FP8 / FP16 / BF16 / TF32 / INT8 / FP64；累加器用 FP32/FP16
- **FP8 两种格式**（首个原生 FP8 芯片）：
  - **E4M3**（4 指数 + 3 尾数）：精度优先 → 权重/激活
  - **E5M2**（5 指数 + 2 尾数）：动态范围优先 → 梯度/KV 缓存
- FP8 数据减半 → **带宽减半、吞吐翻倍**
- **2:4 结构化稀疏**：每 4 个元素保留 2 个非零 → 算力翻倍（稀疏模式）

### 3.2 Transformer Engine（Transformer 引擎）

- 软件 + 硬件结合：逐层分析 Tensor Core 输出统计值 → **每层自动决定用 FP8 还是 FP16**，并用动态缩放把数值压进 FP8 表示范围
- 效果：LLM 训练最高 9 倍、推理最高 30 倍于 A100

### 3.3 DPX 指令

- 新增的**动态规划专用指令**（Smith-Waterman 基因比对、Floyd-Warshall 路径规划等），最高 7 倍加速——说明 H100 连"非神经网络算法"也在专用化

### 3.4 WGMMA（Hopper 的矩阵乘指令语义）

- **WGMMA = Warpgroup MMA**：4 个 warp（128 线程）协同执行一次矩阵乘
- 硬件形状：**M=64**，N=64/128/256，K=16（FP16/BF16）
- 操作数 A、B 从 **SMEM 读**（用 SMEM descriptor 描述基址/stride/swizzle）——**计算直接吃共享内存，不经过寄存器**
- 异步执行：指令提交到 barrier，等待完成
- ⚠️ 累加器在**每线程寄存器**里（TMEM 是下一代 Blackwell 的特性，不是 Hopper）

---

## 4. 访存层次（存储组件的意义）

```text
寄存器堆（256KB/SM，私有，最快，编译器分配）
   │
   ▼
SMEM/L1（256KB/SM，SMEM 可配至 228KB，块内共享）← TMA 搬运目的地
   │
   ▼
L2（50MB，全片共享，硬件管理，分区交叉结构）
   │
   ▼
HBM3（80GB，5 栈，5120-bit，3.35 TB/s）← 唯一"慢"的外部层
```

| 层 | 容量 | 管理 | 关键设计 |
| --- | --- | --- | --- |
| 寄存器堆 | 256KB/SM | 编译器 | 线程私有，容量决定占用率 |
| SMEM/L1 | 256KB/SM（SMEM 可配至 228KB） | 软件显式 | **swizzle 布局防 bank 冲突**；数据复用的核心 |
| L2 | 50MB | 硬件缓存 | 分区交叉（partitioned crossbar）就近缓存；支持压缩；有保留控制 |
| HBM3 | 80GB | 软件 | 5 个 HBM3 栈，5120-bit；3,352 GB/s；支持压缩；SECDED ECC + 坏行重映射 |

**设计动机**：算力:带宽 ≈ 1,979 TFLOPS : 3.35 TB/s ≈ **590:1**（FP8 稠密）——DRAM 根本喂不饱算力，所以整套架构围绕"**数据在片上复用、尽量少访 DRAM**"设计。这正是参考 H100 做 NPU 时最该抄的设计哲学。

---

## 5. 数据流动（重点）—— 一次 GEMM 的完整旅程

以一次大矩阵乘（AI 算子的核心）为例，看数据如何走完整个流水：

```text
① 主机把权重/输入写进 HBM（全局内存）
② TMA 用 descriptor（张量维度+块坐标，无需逐元素寻址）
   从 HBM 异步搬一块数据 → SMEM          ← 硬件搬运，不占线程
③ WGMMA 从 SMEM 读 A/B 块 → Tensor Core 做 MMA
   累加器 = 每线程寄存器                     ← 计算吃 SMEM，数据复用
④ 结果：寄存器 → SMEM
⑤ TMA 把结果写回 HBM（或留 SMEM 供下一算子融合使用）
⑥ 关键：②的下一块数据与③的计算同时进行（异步流水线/双缓冲）
   → 搬运与计算重叠，Tensor Core 不空等数据
```

**三个层面的数据流**：

| 层面 | 数据怎么流 | 省什么 |
| --- | --- | --- |
| 片内块级 | HBM → TMA → SMEM → Tensor Core → SMEM → TMA → HBM | SMEM 块复用，块内多次消费 |
| SM 间 | Cluster 内 SM↔SM 走 DSMEM（直连 SM-to-SM 网络），不经过全局内存 | 比走全局内存快约 7 倍 |
| 片间/层间 | 层与层之间经 HBM/L2 流转；跨 GPU 走 NVLink | 显存带宽 |

**编程模型层面**（warp specialization，生产者-消费者）：
- 生产者 warp：发 TMA 指令搬数据（单线程发起）
- 消费者 warp：等 mbarrier → 算 WGMMA
- 同步靠 barrier，不靠轮询 → 搬运与计算全程重叠

---

## 6. 同步与协作原语（Hopper 的"新语言"）

| 原语 | 是什么 | 意义 |
| --- | --- | --- |
| **mbarrier（异步屏障）** | 分步同步：线程 `Arrive`（非阻塞）→ 需要时 `Wait` | 早到的线程干别的活，屏障"免费"化 |
| **异步事务屏障** | 在 mbarrier 基础上**计数事务（字节数）** | TMA 搬运完成 = 字节数到齐 → 数据就绪可消费 |
| **Thread Block Cluster** | 多个线程块（≤16）保证并发调度到 GPC 内相邻 SM | 跨 SM 协作的最小单位 |
| **DSMEM（分布式共享内存）** | Cluster 内线程直接读写其他 SM 的 SMEM | SM 间数据交换不经全局内存，快约 7 倍 |

> 对验证的意义：这些原语就是你们"翻译层"要对的语义——尤其**事务计数 barrier**，它是 TMA 完成判定的硬件机制。

---

## 7. 对外接口

| 接口 | 规格 | 用途 |
| --- | --- | --- |
| **HBM3** | 80GB / 5 栈 / 5120-bit / ~3 TB/s | 显存（计算数据） |
| **NVLink 4** | 18 链路 × 25GB/s/链路 = **900 GB/s** | GPU↔GPU（多 GPU 共享内存） |
| **NVSwitch 3 代** | 单芯片 64 端口，13.6 Tbit/s | 节点内全互联（DGX 8 GPU 无阻塞） |
| **NVLink Switch System** | 最多 **256 GPU**，57.6 TB/s 全对全带宽 | 跨节点规模扩展（2:1 胖树） |
| **PCIe Gen5** | x16，128 GB/s | 主机接口（CPU/DPU），SR-IOV 虚拟化 |

**NVLink 与 PCIe 的定位差异**：NVLink 是 GPU 之间的一致性、无损、低延迟专用互联（7 倍于 PCIe 带宽），PCIe 只管主机侧。

---

## 8. 对自研 NPU 的启示（组件意义映射）

| H100 组件 | 本质作用 | 自研 NPU 对应物 | 验证重点 |
| --- | --- | --- | --- |
| SM + warp 调度 | 多执行单元 + 指令调度 | 计算簇/执行阵列 | 调度语义、并发时序 |
| Tensor Core + WGMMA | 大矩阵乘引擎（SMEM 直连） | MMA 引擎（M64×N×K16 契约） | 指令级 golden：形状/累加/精度 |
| SMEM 228KB | 显式管理片上存储（数据复用） | 大 SRAM 缓冲 + swizzle | bank 冲突、布局正确性 |
| TMA + descriptor | 异步批量搬运（不占线程） | 专用 DMA 引擎 | descriptor 解析、地址生成 |
| mbarrier/事务屏障 | 异步完成判定 | 同步原语 | 事务计数、时序正确性 |
| Cluster/DSMEM | 跨单元协作 | 片上网络（NoC） | 多核互访、一致性 |
| FP8 + 稀疏 | 低精度省带宽省算力 | 低精度计算路径 | 精度/容差（atol/rtol） |

**明确不继承**：图形管线、完整 FP64 通用性、完整 SIMT 调度——NPU 保留"计算引擎 + 片上存储 + 异步搬运"骨架即可。

---

## 9. 验证工程师的 H100 认知清单

1. **物理层级**：GPC → TPC → SM（132 个），SM 是基本调度/执行单元
2. **warp = 32 线程**，SIMT：一条指令 32 线程并行
3. **WGMMA 三件套**：SMEM descriptor → 异步 MMA → mbarrier 等待
4. **TMA 语义**：descriptor + 块坐标 + 事务计数 barrier
5. **访存五层**：RF → SMEM/L1(256KB) → L2(50MB) → HBM(3.35TB/s)，带宽逐层差一个量级
6. **FP8 两格式**：E4M3（精度）/ E5M2（范围）；2:4 稀疏路径要与稠密路径验证一致
7. **算力:带宽 ≈ 650:1**——凡是"喂不饱计算"的性能问题，先查数据复用和异步流水

---

## 10. 附录：TS 访存与 SM 访存（FPGA 验证链路视角）

> 背景：项目讨论中出现"TS 访存 / SM 访存"（两者均非 NVIDIA 官方术语）。按 H100 参考架构语义，二者对应验证链路中**两条不同的访存路径**。

### 10.1 语义对齐（以"PC 写数据 → 配寄存器 → NPU 取数 → 计算 → 写回 → 校验"链路为例）

| 术语 | 链路位置 | 对应硬件 | 访存粒度/机制 |
| --- | --- | --- | --- |
| **TS 访存** | 取数阶段：NPU 从主存取数据 | 取数引擎（≈ H100 的 TMA / NVDLA 的 CDMA） | 张量块粒度，硬件生成地址 |
| **SM 访存** | 计算阶段：计算单元访问片上缓冲 | SM 的 LD/ST 指令 + WGMMA 从 SMEM 读操作数 | 指令/算子粒度 |

> 同事的说法："NPU 去主存取数据的阶段起作用"——对 **TS 访存**完全正确；SM 访存严格说发生在紧接着的**计算阶段**（若内部把 SM 定义为片上存储，则取数阶段"数据落缓冲"也属 SM 访存）。

### 10.2 TS 访存机制（取数阶段）

```text
软件写配置寄存器：源地址(主存)、块大小、维度/步长、目标片上缓冲、启动位
   │
   ▼
取数引擎（DMA）：
   ① 按"张量块"语义自动生成读地址（基址 + 块坐标 × 各维步长，非逐元素寻址）
   ② 发 AXI 读事务（AR 通道）→ 主存数据经 R 通道返回
   ③ 数据写入片上缓冲（SMEM 类似物）
   ④ 块就绪 → barrier/握手信号通知计算单元
   ⑤ 地址按步长推进，取下一块，直到整个张量取完
```

关键：取数过程**不占计算单元**——软件只写一次配置，地址生成与搬运全部硬件自动完成。

### 10.3 SM 访存机制（计算阶段）

```text
计算单元收到"块就绪"通知：
   ① 从片上缓冲读操作数（A/B 数据、权重）
   ② 计算（累加器在寄存器）
   ③ 结果写回片上缓冲（或经写回 DMA 直接到主存）
   ④ 缓冲腾空 → 通知取数引擎搬下一块（流水线重叠）
```

**核心区别**：TS 访存管"外部"（主存 ↔ 片上缓冲，块粒度、硬件寻址）；SM 访存管"内部"（片上缓冲 ↔ 计算单元，指令粒度）。

### 10.4 两种主存的访问路径

| 主存形态 | 路径 | 特点 |
| --- | --- | --- |
| FPGA 上的 DDR | NPU DMA → **AXI** → DDR 控制器 | 高带宽、低延迟、确定性；地址 = DDR 物理地址 |
| PC 侧存储 | 必须经 **PCIe** 桥接：① PC 先写数据到 FPGA DDR（staging）再取（最常用）② FPGA 侧 PCIe DMA 引擎直取 | 带宽受 PCIe 限制，延迟高 |

### 10.5 验证检查点

- **TS 访存**：抓 AXI 读事务（AR/R）——地址是否符合"基址+步长"配置、块长度是否正确
- **SM 访存**：抓片上缓冲→计算单元通路——数据顺序/布局（swizzle、bank、字节序）
- **两种主存各测一遍**：DDR 路径验 AXI 时序；PC 内存路径额外验 PCIe 搬运环节

### 10.6 实例：测试 case"计算 1+2"的完整链路

假设：数据为 int32（4 字节），主存起始地址 `0x0000_0000`，片上缓冲称 SMEM：

```text
① PC 写数据到主存
   主存[0x0000_0000] = 1
   主存[0x0000_0004] = 2

② 软件写配置寄存器（CSB 通路）
   源地址=0x0000_0000 ｜ 元素数=2 ｜ 目标=片上缓冲
   算子=ADD ｜ 写回地址=0x0000_0008 ｜ 启动位=1

③ TS 访存 ← 取数阶段（同事说的阶段）
   取数引擎（DMA）：
   · 按"基址+步长"生成地址：0x0000_0000, 0x0000_0004
     （连续元素步长=4 字节；多维张量则按各维 stride 跳）
   · 发 AXI 读事务 → 主存返回：1, 2
   · 写入片上缓冲：buf[0]=1, buf[1]=2
   · 块就绪 → 通知计算单元

④ SM 访存 + 计算 ← 计算阶段
   计算单元：
   · 从片上缓冲读操作数：op0=buf[0]=1, op1=buf[1]=2   ← SM 访存
   · 执行 ADD：1 + 2 = 3
   · 结果写回：result=3

⑤ 写回
   写回引擎：AXI 写事务 → 主存[0x0000_0008] = 3 → 中断通知 PC

⑥ 校验
   PC 从主存读回 3 → 与期望值 3 比对 → PASS
```

**要点**：

1. 1+2 只触发一次 AXI burst（2 元素 × 4 字节）——机制与大型算子完全一致，只是块小、无步长跳变
2. 1+2 走**标量/向量 ALU 通路**（非 Tensor Core 的 MMA），但"取数→缓冲→计算→写回"骨架相同——正因如此它可作为**通路最小测试 case**
3. SM 访存 = 计算单元读操作数的动作（`ld` 指令语义）；TS 访存 = 取数引擎从主存取块的整套动作
4. 校验时除数值外还要比**数据格式/字节序**（由②的数据格式寄存器决定）

---

## 11. 资料来源

- [NVIDIA H100 GPU Whitepaper（官方白皮书 PDF，本目录已有下载）](nvidia-h100-tensor-core-hopper-whitepaper.pdf)——本文主要依据
链接：https://resources.nvidia.com/en-us-hopper-architecture/nvidia-h100-tensor-c
- [NVIDIA Hopper Architecture In-Depth（官方技术博客，白皮书网页版）](https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/)
- [Deep Dive on the Hopper TMA Unit（PyTorch）](https://pytorch.org/blog/hopper-tma-unit/)
- [PTX ISA 文档（wgmma / cp.async.bulk / mbarrier 指令语义）](https://docs.nvidia.com/cuda/parallel-thread-execution/)
- [NVIDIA H100 官方规格页（最终上市数字）](https://www.nvidia.com/en-us/data-center/h100/)
