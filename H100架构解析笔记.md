# H100（Hopper）架构解析笔记

> 学习目的：H100 是自研 NPU 的参考架构，需要吃透其 SM / Tensor Core / 访存层次 / 异步数据搬运设计
> 创建日期：2026-08-12
> 规格来源：NVIDIA H100 Whitepaper、Hopper In-Depth 技术博客、公开资料

---

## 0. 定位与一句话总结

**H100 = Hopper 架构的数据中心 GPU**（2022 年发布，GH100 芯片）。它是"GPU NPU 化"的集大成者：在通用 SIMT 核旁边放上**大规模专用 Tensor Core + 异步数据搬运引擎（TMA）**，AI 计算能力（FP8 3,958 TFLOPS）是通用计算（FP32 67 TFLOPS）的 **59 倍**——这正好解释了"自研 NPU 参考 H100"的逻辑：**H100 的 AI 计算部分，本质上就是一个巨型 NPU 与通用 GPU 的组合**。

---

## 1. 关键规格（H100 SXM5，旗舰型号）

| 项 | 数值 |
|---|---|
| 架构 / 芯片 | Hopper（GH100），TSMC 4N，约 800 亿晶体管 |
| SM 数量 | 132 个（全片 144 个） |
| CUDA 核心 | 16,896（每 SM 128 个） |
| Tensor Core | 528 个（每 SM 4 个，第 4 代） |
| FP8 张量算力 | **3,958 TFLOPS** 稠密 / 7,916 稀疏 |
| FP16/BF16 | 1,979 TFLOPS / 3,958 稀疏 |
| TF32 | 989 TFLOPS |
| INT8 | 3,958 TOPS 稠密 |
| FP32 / FP64 | 67 / 34 TFLOPS |
| 显存 | 80GB HBM3，**3.35 TB/s** |
| L2 缓存 | 50MB |
| NVLink 4 | 900 GB/s（每 GPU） |
| TDP | 700W |
| MIG 实例 | 最多 7 个 |

**版本差异**（SXM vs PCIe）：PCIe 版 114 SM、350W、HBM3 2.0 TB/s、NVLink 600 GB/s。

**算力:带宽比**：989 TFLOPS（FP16）÷ 3.35 TB/s ≈ **295:1** —— 光靠 DRAM 不可能喂饱算力，这就是片上数据复用和异步搬运设计存在的根本原因。

---

## 2. SM：基本处理单元（架构核心）

```
                    ┌───────────── SM（流多处理器）─────────────┐
                    │  256KB 寄存器堆                          │
   ┌──────┐         │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
   │warp  │────────▶│  │子分区 0 │ │子分区 1 │ │子分区 2 │ │子分区 3 │
   │调度器│  每分区: │  │1 warp调度│ │1 warp调度│ │1 warp调度│ │1 warp调度│
   └──────┘  1 指令 │  │32 FP32  │ │32 FP32  │ │32 FP32  │ │32 FP32  │
   /周期          │  │16 FP64  │ │16 FP64  │ │16 FP64  │ │16 FP64  │
                    │  │1 TensorCore│ │1 TensorCore│ │1 TensorCore│ │1 TensorCore│
                    │  └─────────┘ └─────────┘ └─────────┘ └─────────┘
                    │  228KB 共享内存 / L1（可配置） + 32 个 LD/ST 单元
                    │  TMA 引擎（异步批量搬运）   ── 每 SM 一个
                    └──────────────────────────────────────────────┘
```

| 组成 | 数量/容量 | 说明 |
|---|---|---|
| 处理子分区 | 4 个 | 每个：1 个 warp 调度器 + 1 个发射单元 |
| FP32 核心 | 128（4×32） | CUDA 通用标量计算 |
| FP64 核心 | 64（4×16） | 双精度（HPC 用） |
| Tensor Core | 4（每子分区 1 个） | 第 4 代，矩阵乘加 |
| 寄存器堆 | **256KB** | 线程私有 |
| SMEM/L1 | **228KB** | 可配置拆分（共享内存 vs 缓存），块内共享 |
| LD/ST 单元 | 32 个 | 访存指令执行 |
| TMA 引擎 | 1 个 | 异步张量搬运（Hopper 新特性） |

**执行模型**：SIMT，一个 warp = 32 线程，每周期每个调度器发一条 warp 指令（32 线程同时执行）。

---

## 3. Tensor Core 与 WGMMA（AI 计算引擎）

### 3.1 第 4 代 Tensor Core

- **首个原生支持 FP8 的芯片**：E4M3（权重/激活，精度高）与 E5M2（梯度/KV 缓存，动态范围大），配合 **Transformer Engine** 按层自动切换 FP8/FP16
- 支持精度：FP8 / FP16 / BF16 / TF32 / INT8 / FP64
- 一次融合运算：`D = A×B + C`，累加用 FP32，输出可转回 FP16/BF16/FP8
- **2:4 结构化稀疏**：每 4 个元素有 2 个非零 → 稀疏算力翻倍（7,916 TFLOPS）
- FP8 比 BF16 每个参数省 1 字节 → **FP8 同时减半 HBM 带宽压力**（这是推理 NPU 设计的关键动机）

### 3.2 WGMMA（Hopper 的矩阵乘指令）

**注意（重要澄清）**：**TMEM（Tensor Memory）是 Blackwell（SM100）的特性，不是 Hopper 的**——Hopper 的 MMA 累加器放在**每线程寄存器**里，通过 WGMMA 指令访问。

WGMMA = Warpgroup Matrix Multiply-Accumulate，Hopper 的矩阵乘语义：

| 特性 | 说明 |
|---|---|
| 参与线程 | warpgroup = 4 个 warp = **128 线程**协同 |
| 硬件形状 | **M=64 固定**；N = 64/128/256；K=16（FP16/BF16） |
| 操作数来源 | A、B 从**共享内存（SMEM）**读，用 SMEM descriptor 描述（基址 + stride + swizzle 模式） |
| 累加器 | 每线程寄存器中，跨指令累积 |
| 执行方式 | **异步**：指令提交到 barrier，等 barrier 到达才保证完成 |
| 配套指令 | `wgmma.mma_async` + mbarrier 同步 |

**对自研 NPU 的意义**：WGMMA 的形状（M64/N×/K16）和"SMEM descriptor + 异步 + barrier"三件套，就是 H100 暴露给软件（PTX/编译器）的**硬件接口契约**——参考 H100 的 NPU 的 MMA 引擎，本质上就是按这个契约实现，你们的工具链翻译层输出的也正是这些指令语义。

---

## 4. 访存层次（数据从哪来、去哪）

```
线程寄存器堆（256KB/SM，私有，最快）
    │
    ▼
共享内存 SMEM / L1（228KB/SM，块内共享，显式管理）
    │  TMA 异步搬运 / LD/ST
    ▼
L2（50MB，全片共享，硬件管理）
    │
    ▼
HBM3（80GB，3.35 TB/s，外部存储）
```

| 层 | 容量 | 管理方式 | 关键点 |
|---|---|---|---|
| 寄存器堆 | 256KB/SM | 编译器分配 | 线程私有，容量决定占用率 |
| SMEM/L1 | 228KB/SM | 软件显式管理 | **swizzle 布局**避免 bank 冲突 |
| L2 | 50MB | 硬件缓存 | 跨 SM 共享，聚合带宽 |
| HBM3 | 80GB | 软件管理 | 3.35 TB/s，整机唯一"慢"层 |

**设计动机**：算力:带宽 ≈ 295:1，所以 H100 的整套设计（SMEM、TMA、WGMMA 从 SMEM 取数、FP8 省带宽）都是围绕"**减少 DRAM 访问次数**"——和你学过的 NVDLA 的 CBUF/三级数据复用是同一个思想，只是规模和组织形式不同。

---

## 5. TMA：张量内存加速器（Hopper 最大创新之一）

**TMA = Tensor Memory Accelerator**，每 SM 一个的异步 DMA 引擎，专门搬运张量数据：

| 特性 | 说明 |
|---|---|
| 功能 | 全局内存 ↔ 共享内存之间搬**多维张量**（1D~5D），不阻塞计算 |
| 编程方式 | **descriptor 驱动**：tensor map（维度/stride）+ 块坐标，无逐元素指针运算 |
| 发起 | 单线程发起（如 `cuda::memcpy_async`），多线程在共享内存的 **mbarrier** 上等待 |
| PTX 指令 | `cp.async.bulk.tensor` |
| 同步原语 | **异步事务屏障（mbarrier）**：同时计数"线程到达"和"字节事务"，完成才放行 |
| 配套模型 | **warp specialization**：生产者 warp 发 TMA，消费者 warp 算 WGMMA，流水线重叠 |

**实测效果**（Triton FP8 GEMM 对比）：全局内存吞吐 910 GB/s → **1.45 TB/s（+59%）**。

**对自研 NPU 的意义**：TMA 就是"NPU 的 DMA 单元"的 GPU 版形态——descriptor 驱动 + 事务计数同步，是你们验证"翻译层 + 数据通路"时必须对的语义。

---

## 6. Thread Block Cluster 与 DSMEM（跨 SM 协作）

- **Thread Block Cluster**：多个线程块组成簇（最多 16 个块 / 16 个 SM），块间可协作
- **DSMEM（分布式共享内存）**：块可以通过簇直接读写**其他 SM 的共享内存**（SM-to-SM 直连），无需经过 L2
- 典型应用：FlashAttention-4 的分块注意力，跨 SM 交换 KV 块
- 硬件基础：片上互连把 SM 连成可寻址簇网络

---

## 7. 系统级互连

| 互连 | 规格 | 用途 |
|---|---|---|
| NVLink 4 | **900 GB/s**/GPU | GPU↔GPU（DGX H100 通过 NVSwitch v3 无阻塞全互联） |
| NVLink C2C | 900 GB/s | CPU↔GPU 一致性互连（Grace Hopper） |
| PCIe Gen5 | 128 GB/s | 通用主机接口 |
| HBM3 | 3.35 TB/s | 显存带宽 |

---

## 8. 对"参考 H100 的自研 NPU"的启示（重点）

参考 H100 不等于复刻 GPU——而是**继承其 AI 计算相关的设计决策**。按价值排序：

| H100 设计 | 自研 NPU 可继承的点 | 验证重点（对应你们项目） |
|---|---|---|
| SM + warp 调度 | 多执行单元 + 指令调度器 | warp 语义、发射/调度时序 |
| Tensor Core + WGMMA | MMA 引擎：形状（M64×N×K16）+ 累加器 | **指令级 golden**：形状、累加、精度 |
| SMEM 228KB + swizzle | 显式管理大片上 SRAM | bank 冲突、swizzle 正确性 |
| TMA + mbarrier | 异步 DMA + 事务计数同步 | **descriptor 解析、地址生成、同步时序** |
| 寄存器累加（非 TMEM） | 累加器放哪、怎么访存 | 寄存器压力/占用率（如果做通用核） |
| FP8 + 2:4 稀疏 | 低精度计算 + 结构化稀疏 | 精度/容差（对比 golden 用 atol/rtol） |
| 算力:带宽 295:1 | 片上复用设计目标 | 吞吐测试：实际达到峰值的百分比 |

**同时要明确不继承什么**：完整的 FP32/FP64 通用核、图形管线、完整的 SIMT 语义——这些是 GPU 的"通用性成本"，NPU 通常砍掉或大幅简化。

**与已有笔记的衔接**：
- [NVIDIA工具链与自研NPU验证笔记.md](NVIDIA工具链与自研NPU验证笔记.md) 里的"兼容层次"，落到 H100 就是：你们翻译层处理的指令语义 = **WGMMA / TMA / mbarrier / 访存指令** 这一族 Hopper 指令
- [CUDA软件栈概念笔记.md](CUDA软件栈概念笔记.md) 的 SASS 层，H100 上就是这些指令的机器码形态

---

## 9. 验证工程师的 H100 认知清单

1. **一条 warp = 32 线程**，SIMT 语义是翻译层正确性的前提
2. **WGMMA 三件套**：SMEM descriptor → 异步 MMA → mbarrier 完成等待
3. **TMA 语义**：descriptor + 块坐标 + 事务计数，检查地址生成
4. **访存五层**：RF → SMEM → L2 → HBM，每层的容量/带宽决定验证数据的量级
5. **FP8 两格式**：E4M3（精度）/ E5M2（范围），量化链路容差设计的依据
6. **2:4 稀疏**：稀疏模式与稠密算力 2:1，验证稀疏路径与稠密路径一致性

---

## 10. 学习资源

- [NVIDIA H100 Whitepaper（官方白皮书）](https://resources.nvidia.com/en-us-tensor-core)（含 SM 结构、Tensor Core、访存层次的权威描述）
- [NVIDIA Hopper Architecture In-Depth（官方技术博客）](https://developer.nvidia.com/blog/nvidia-hopper-architecture-in-depth/)
- [Deep Dive on the Hopper TMA Unit（PyTorch 博客）](https://pytorch.org/blog/hopper-tma-unit/)
- [PTX ISA 文档（wgmma / cp.async.bulk / mbarrier 指令语义）](https://docs.nvidia.com/cuda/parallel-thread-execution/)
- [CUTLASS（WGMMA/TMA 参考实现）](https://github.com/NVIDIA/cutlass)
- [NVIDIA H100 CUDA Performance Guide](https://www.rightnowai.co/guides/gpu-comparison/h100)
