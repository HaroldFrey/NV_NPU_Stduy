# NVIDIA 工具链与自研 NPU 验证笔记

> 面向：FPGA 原型验证工程师（自研 NPU，兼容 NVIDIA 工具链）
> 创建日期：2026-08-12
> 背景：项目测试 case 走 NVIDIA 编译链路（CUDA / TensorRT），需要理解链路的每个环节、各环节的产物、以及验证检查点

---

## 0. 一句话定位

**测试 case = 走 NVIDIA 编译链路生成的二进制，被加载到自研 NPU 的 FPGA 原型上执行，结果与 GPU golden 比对。**

验证工程师的工作，就是在这条链的每个环节设检查点，出了问题能**定位到具体是哪一层**（编译 / 翻译 / 驱动 / 硬件）。

---

## 1. 全链路总览（你项目的形态）

```
┌────────── 测试 case 源 ──────────┐
│ ① 算子级：手写 CUDA C kernel      │
│ ② 网络级：公开模型 → TensorRT     │
└──────────────┬───────────────────┘
               ▼
        nvcc / TensorRT builder         ← NVIDIA 官方工具链（闭源）
               ▼
   PTX / SASS / CUBIN / .engine          ← 编译产物（含中间文件）
               │
               ▼
   自研加载器 / 运行时 / 驱动              ← 你们自己的软件层（关键兼容层）
               │
               ▼
    自研 NPU 指令 / 寄存器配置
               │
               ▼
        FPGA 原型（硬件执行）
               │
               ▼
   结果 与 GPU 上同 case 的输出比对        ← golden 参照
```

**核心认知**：NVIDIA 工具链的产物（cubin / engine）不是给硬件直接执行的——中间隔着你们的兼容层。**这个兼容层 + 硬件，就是你验证的全部对象。**

---

## 2. "兼容 NVIDIA 工具链"的三种兼容层次（先认清你们是哪一种）

兼容层次决定了：case 以什么形态进入你们的硬件、验证重点在哪。请对号入座：

| 兼容层次 | 含义 | 你们要做的 | 验证重点 |
|---|---|---|---|
| **A. 应用/API 级** | 兼容 CUDA runtime API / TensorRT API，内部完全自研 | 运行时实现 API 语义 | API 行为、内存模型、错误处理 |
| **B. 中间表示级** | 吃 **PTX** / ONNX，自研翻译器映射到自研指令 | 指令翻译器（转译） | **PTX 指令语义覆盖**、翻译正确性 |
| **C. 指令级** | 直接执行 **SASS** 子集 | 硬件做 SM 类似物 | 指令语义、微架构行为 |

- 绝大多数自研 NPU 在 **A 或 B**；C 接近"造 GPU"，工程量完全不同
- 判断方法：看你们运行时加载的是 `.cubin`（C）、`PTX 文本`（B），还是上层 API 调用（A）
- **兼容层次不一定是纯一种**——很多产品是 A+B 混合（API 兼容 + PTX 翻译）

---

## 3. 编译链路逐环节拆解（测试 case 的旅程）

### 3.1 源：测试 case 从哪来

| 类型 | 形态 | 用途 |
|---|---|---|
| 算子级 | 手写 CUDA kernel（conv/GEMM/softmax…） | 单算子验证、边界参数 |
| 网络级 | 公开模型（ResNet-50 等）→ ONNX → TensorRT | 端到端验证 |
| 随机生成 | fuzz kernel / 随机 tensor 尺寸 | 覆盖率、异常路径 |
| 回归集 | 历史 bug 复现 case | 防回退 |

### 3.2 nvcc 编译：CUDA C → PTX → SASS → CUBIN

```
CUDA C → (nvcc 前端) → PTX → (ptxas) → SASS → 打包 → CUBIN
                     ↑ 可保留       ↑ 可反汇编      ↑ 可解析
```

- 用 `nvcc -keep` 保留每步中间文件；`cuobjdump` 看 cubin 内容；`nvdisasm` 反汇编 SASS
- 每个产物都是**可检查的中间证据**——验证时拿它们对照翻译器输出

### 3.3 TensorRT 路径（推理 NPU 常见）

```
模型(ONNX/Caffe) → TensorRT builder → .engine 文件 → TensorRT runtime → 推理
```

- `.engine` = 序列化的网络图 + 算子实现选择（tactics）+ 权重
- `trtexec` 是官方测试工具：可以一键跑整网、测性能、导出中间层输出

### 3.4 自研加载器 / 翻译器（你们的核心兼容层）

- 读入 cubin / PTX / engine → 翻译成自研指令或寄存器配置
- **这是"兼容"成败的关键**：NVIDIA 语义 → 你们硬件的映射

### 3.5 硬件执行 + 结果比对

- 硬件跑完后结果写回（AXI 数据通路），中断通知
- 与 **GPU 真卡**上跑同一个 case 的输出比对（golden）

---

## 4. 验证检查点清单（逐层设卡）

| 层 | 检查什么 | 工具/方法 |
|---|---|---|
| 编译产物 | 编译器是否生成正确指令（对照 PTX 语义） | `nvdisasm` / `cuobjdump` / PTX 阅读 |
| 翻译器输出 | 自研指令是否等价于 PTX/SASS 语义 | 指令级 diff、语义仿真器 |
| 驱动/寄存器 | CSB 配置是否正确、DMA 地址/事务合法 | 波形抓取（ILA）、APB/CSB 比对 |
| AXI 数据通路 | 数据搬移正确性、带宽是否达标 | AXI monitor、带宽计数器 |
| 计算结果 | 输出 vs GPU golden，容差内一致 | 数值比对（atol/rtol） |

**定位方法论**：差异出现时从中间二分——先确认翻译器输出正确（软件侧对），再查硬件；或先在 RTL 仿真复现，再上板。

---

## 5. 三层 golden 比对（原型验证的核心方法论）

```
GPU（真卡）─── 期望输出 ──┐
                         ├── 比 对 ── 差异定位
C model / 语义仿真 ──────┤
                         │
FPGA 原型 ───────────────┘
```

- **容差问题要提前定**：GPU 用 FP32/FP16 累加，自研 NPU 实现可能不同 → 数值不可能 bit 级一致，需要 atol/rtol 策略（尤其 INT8 量化链路）
- 每版 RTL 更新后：回归整个 golden 库

---

## 6. 你需要掌握的能力清单（按优先级）

1. **读 PTX / SASS**：能看懂翻译器输入是什么语义（warp 同步、内存模型、指令语义）
2. **理解 CUDA 编程模型**：grid/block/warp、全局/共享/局部内存——翻译层的语义源头
3. **数值精度意识**：FP16/INT8 量化、累加顺序差异 → 比对容差设计
4. **驱动/寄存器流程**：CSB 配置、DMA 管理——你学的 AXI/APB 知识直接复用
5. **工具链操作**：nvcc 参数、cuobjdump/nvdisasm、trtexec、TensorRT 部署流程

---

## 7. 与已学知识的关联

| 已学内容 | 在本项目中的用途 |
|---|---|
| [CUDA软件栈概念笔记.md](CUDA软件栈概念笔记.md) | 编译链路的基础词汇（PTX/SASS/CUBIN/compute capability） |
| [NVDLA架构学习笔记.md](NVDLA架构学习笔记.md) | 另一种 NPU 形态（per-layer 寄存器编程 vs CUDA 指令流），理解设计取舍 |
| AXI FULL / APB（此前学习） | 数据通路与配置通路的验证手段 |
| CSB / 乒乓寄存器（NVDLA） | 理解"配置"类硬件的编程模型 |

**两种 NPU 形态对照**（理解你们硬件定位的框架）：

| | NVDLA 式 | CUDA 兼容式（你们的形态） |
|---|---|---|
| 编程模型 | per-layer 寄存器配置 | 指令流（SIMT/warp） |
| 编译产物 | loadable（寄存器配置） | cubin/PTX/engine |
| 硬件执行 | 流水线引擎 | 多执行单元阵列 |
| 灵活性 | 固定算子集 | 通用（取决于兼容层次） |

---

## 8. 下一步建议

- [ ] 确认兼容层次（A/B/C 或混合）——决定验证重点
- [ ] 建立 GPU golden 比对环境（真卡 + 容差策略）
- [ ] case 分级：单指令 → 算子 → kernel → 整网 的递进回归体系
- [ ] 在 RTL 仿真中复现翻译器输出，建立"指令→波形"的映射检查
- [ ] 掌握工具链三件套：nvcc / cuobjdump+ nvdisasm / trtexec
